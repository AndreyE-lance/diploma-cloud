? 1) Действительно ли каждому пользователю нужна своя таблица?
    Как вы будете создавать таблицы, если появится функционал регистрации?
    На самом деле, все файлы всех пользователей можно хранить в одной таблице и
    просто иметь у каждого файла ссылку на пользователя (на его id).

DONE 2) Переименовал файл и попробовал скачать – загрузка упала. Физический файл на
    диске не переименовался

DONE  3) Обращайте внимание на подсказки IDEA. Вот в этом фрагменте:
    System.out.println(authResult.isAuthenticated());
    if(authResult!=null){
    проверка if бесполезна, т.к. authResult на данном этапе уже не будет
    null, т.к. вы к ней уже
    обратились и выпал бы NPE ещё строкой выше.

DONE 4)} catch (Exception e){//тут нужны эксепшны
    System.out.println(e.getMessage());
    }
    Нужны 🙂Добавим?

DONE 5) Не увидел никакого логгирования. Давайте добавим.

6) Хотелось бы README в репозиторий добавить